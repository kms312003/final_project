<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="mapper.ssd">
     
     <select id = "getSSDCount" resultType="int">
        select ifnull(count(*),0) from ssd
     </select>
     
     <select id = "getSSDs" parameterType="hashmap" resultType="ssd">
        select * from ssd limit #{start}, #{end} 
     </select>
     
     <insert id="insert" parameterType="ssd">
        insert into ssd(id, productName, productCompany, brand, socket,
               core, thread, clockSpeed, tdp, productDate, regDate, price, count, filename, filesize) 
               values(0, #{productName},#{productCompany},#{brand},#{socket},#{core}
      ,#{thread},#{clockSpeed},#{tdp},#{productDate},curdate(),#{price},#{count}, #{filename},#{filesize})
     </insert>
     
     <select id="getSSD" resultType="ssd" parameterType="hashmap">
        select * from ssd where id = #{id}
     </select>
     
     <update id="readCount" parameterType="hashmap">
        update ssd set readcount = readcount + 1 where id = #{id}
     </update>
     
     <update id="update" parameterType="ssd">
        update ssd set productName=#{productName}, productCompany=#{productCompany}, brand=#{brand}, 
        socket= #{socket}, core=#{core}, thread=#{thread}, clockSpeed=#{clockSpeed}, tdp=#{tdp}, productDate=#{productDate}, price=#{price}, filename=#{filename}, filesize=#{filesize} where id=#{id}
     </update>
     
     <delete id="deleteSSD" parameterType="hashmap">
        delete from ssd where id=#{id}
     </delete>
  </mapper>