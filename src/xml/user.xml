<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.user">
	<insert id="insert" parameterType="user">
		insert into 
		userinfo (id, email, name, password, gender, birth, job, reg_date)
		values 
		(0, #{email}, #{name}, #{password}, #{gender}, #{birth},#{job},curdate())
	</insert>

	<select id="getPassword" parameterType="hashmap" resultType="String">
		select password from userinfo where email=#{email}
	</select>
	
	<select id="getEmail" parameterType="hashmap" resultType="String">
		select email from userinfo where email = #{email}
	</select>
</mapper>